import React from 'react'

import { readData, saveData, color } from '../helpers/helperFunc'

describe('All functions should return correct values', () => {
  it('color returns the correct color', () => {

  })

  it.skip('saveData should update the async storage', () => {

  })

  it.skip('readData should obtain all the data in the correct shape', () => {

  })
})
